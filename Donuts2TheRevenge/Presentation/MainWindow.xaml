<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ComboBox Margin="10,10,484,379" ItemsSource="{Binding Path=Donuts}" DisplayMemberPath="donut_name" Grid.Row="1"
                  SelectedItem="{Binding Path=CurrentDonut, Mode=TwoWay}"/>

        <Grid Grid.Row="1" Margin="426,10,44,355">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="11*"/>
                <RowDefinition Height="11*"/>
            </Grid.RowDefinitions>
            <TextBox Text="{Binding CurrentDonut.donut_name}" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" />
            <TextBox Text="{Binding CurrentDonut.filling}" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" />
        </Grid>
    </Grid>
</Window>
